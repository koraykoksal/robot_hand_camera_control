# Kamera + el takibi
opencv-python>=4.8
mediapipe>=0.10.9
numpy>=1.24

# Robot SDK (EtherCAT) — vendor örneğinden
pysoem~=1.1.12
# NOT (Windows): pysoem'in çalışması için Npcap kurulu olmalı.
#   x64/bin/npcap-1.82.exe ile kur (kurulumda "WinPcap API-compatible" seçili olsun),
#   ardından bilgisayarı yeniden başlat.

# keyboard~=0.13.5   # sadece vendor main.py örneği için; bu projede gerekmiyor
